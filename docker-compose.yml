services:
  soc-toolkit:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/soc_toolkit.db
    volumes:
      - ./data:/app/data
